/*let profileview = document.getElementById();
let resume = document.getElementById("getresume");

let count = localStorage.getItem('count') || 0;
profileview.textContent = count;

resume.addEventListener('click', () => {
    count++;
    localStorage.setItem('count', count);
    profileview.textContent = count;
});*/